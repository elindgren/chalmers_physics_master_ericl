file = fopen("velo.dat","w");
    // current_time = 0.0;  // Timestamp for output file
    // for (i = 0; i < nbr_timesteps + 1; i++) {
	// 	current_time = i * dt;
    //     fprintf(file, "%.4f", current_time);
    //     for(j=0; j < nbr_particles; j++){
    //         fprintf(file, " \t%e", velocities[i][j]);
    //     }
	// 	fprintf(file, "\n");
	// }
	// fclose(file);